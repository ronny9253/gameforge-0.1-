#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
case "${1:-}" in
  validate)
    exec python3 "$ROOT/tools/gameforge_validate.py" "$ROOT"
    ;;
  *)
    echo "Usage: ./tools/gameforge validate"
    exit 2
    ;;
esac
